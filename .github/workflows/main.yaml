name: Main

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  analyze:
    uses: ./.github/actions/analyze.yaml@main
    secrets: inherit
  danger:
    uses: ./.github/actions/danger.yaml@main
    secrets: inherit
  test:
    uses: ./.github/actions/test.yaml@main
    secrets: inherit
